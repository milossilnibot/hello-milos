# Hello Milos Flutter App

A simple Flutter app that displays a greeting when a button is pressed.

## Features

- Material Design UI
- Button that shows "Pozdrav, Miloše! 🦊"
- Clean and minimal

## How to Run

1. Ensure you have Flutter installed (`flutter doctor`)
2. Clone this repository
3. Run `flutter pub get`
4. Run `flutter run` (connected device/emulator required)

## Build APK

```bash
flutter build apk --release
```

The APK will be generated at `build/app/outputs/flutter-apk/app-release.apk`.

## License

MIT